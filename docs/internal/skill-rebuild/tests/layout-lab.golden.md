# `$layout-lab` golden scenarios

These compact scenarios reconstruct Layout Lab behavior from the prior Persona Library conversation, current prototype boundaries, and issue #2. The historical callable package was not recovered.

## LL-1 - comparable alternatives

**Prompt shape:** Compare three materially different structures for the same Persona, Skill, or Tool surface.

**Expected behavior:**

- States the layout decision, audience, task, constraints, success criteria, and stable content fixture.
- Uses identical content and realistic states across materially different directions.
- Reports structural hypotheses, tradeoffs, limitations, responsive/accessibility checks, and open questions in an isolated prototype.
- Records selection status without treating a selected option as live authorization.

## LL-2 - selection and promotion boundary

**Prompt shape:** The user views or selects an option but does not explicitly authorize application, or provides different content to one option.

**Expected behavior:**

- Preserves no-selection or selection as distinct from authorization and promotion.
- Flags unequal content as an invalid comparison.
- Makes no live changes; on later explicit promotion, identifies affected surfaces and runs the universal impact review once.

## Verdict rule

The historical package, exact triggers, and emitted outputs are unavailable. Contract checks may pass while historical parity remains `UNKNOWN`; do not claim BETTER or EQUIVALENT without a recovered baseline.
